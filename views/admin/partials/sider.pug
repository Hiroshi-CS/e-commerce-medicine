.sider 
  .inner-menu 
    ul 
      li: a(href=`${prefixAdmin}/dashboard`)
        i.fas.fa-tachometer-alt
        | Tổng quan 
      if(role.permissions.includes("products-category_view"))
        li: a(href=`${prefixAdmin}/products-category`)
          i.fas.fa-list-alt
          | Danh mục sản phẩm
      if(role.permissions.includes("products_view"))  
        li: a(href=`${prefixAdmin}/products`)
          i.fas.fa-box-open
          | Danh sách sản phẩm
      if(role.permissions.includes("roles_view"))
        li: a(href=`${prefixAdmin}/roles`)
          i.fas.fa-users-cog
          | Nhóm quyền
      if(role.permissions.includes("roles_permissions")) 
        li: a(href=`${prefixAdmin}/roles/permissions`)
          i.fas.fa-key
          | Phân quyền
      if(role.permissions.includes("accounts_view")) 
        li: a(href=`${prefixAdmin}/accounts`)
          i.fas.fa-user-friends
          | Danh sách tài khoản 
      li.menu-bottom 
        a(href=`${prefixAdmin}/conversations`)
          i.fas.fa-comments
          | Quản lý hội thoại